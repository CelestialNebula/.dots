#!/usr/bin/env sh
# strftime = format date and time

while true; do
	Date=$(date "+(365/%j) %F | %B %d, %A")
	Clock=$(date "+%R")
	Uptime=$(uptime | sed -E -e 's/^.*up/up/' -e 's/[0-9]+ user.* l/l/' -e '/ /s/  */ /g')
	printf "%s | %s | %s" "$Date" "$Clock" "$Uptime"
	sleep 10
	builtin exit
done
